export function NewRecordPage() {
    return (
        <section>
            <p>
                TODO: From a schema file, generate an empty form. The
                useFormGenerator hook can help.
            </p>
        </section>
    );
}
